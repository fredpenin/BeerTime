{% extends 'base.html.twig' %}

{% block title %}Hello {{ parent() }}!{% endblock %}

{% block body %}

<h1>{{event.name}}</h1>
<img src={{event.poster}} />
<p>Date de début : {{ event.startAt | date() }}</p>
<p>Fin de fin : {{ event.endAt | date() }}</p>
<p>Description : {{ event.content }}</p>
<p>Prix : {{ event.price }}</p>
1
{# {{ dump(app.user) }} #}

{% if app.user %}
    <a href="{{ path('event_join', {id: event.id}) }}">S'inscrire à cet évenement</a>
{% endif %} 




{% endblock %}
